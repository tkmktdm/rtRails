# 追加

 Bootstrap導入

``` sh
yarn add bootstrap
yarn add @popperjs/core
```

``` Gemfile
gem 'cssbundling-rails'
gem "sassc-rails"
gem "bootstrap"
```

``` sh
bundle install
bundle exec rails css:install:sass
```

``` sh
npm i prop-types
```
